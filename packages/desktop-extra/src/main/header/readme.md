```javascript
  <Header
    isLoading={isLoading}
    title={'MyTitle'}
    selectorData={{
      items: agents,
      currentItem: agentName,
    }}
    userName={userName}
    navItems={items}
    onItemChange={setItem}
    onLogout={this.onLogout}
  />
```
