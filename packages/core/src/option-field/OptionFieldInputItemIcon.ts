import {styled, StyledComponent, Theme} from '@qiwi/pijma-core'

const OptionFieldInputItemIcon: StyledComponent<{}, {}, Theme> = styled('div')({
  position: 'absolute',
  top: 0,
  left: 0,
  width: 24,
  height: 24,
})

export default OptionFieldInputItemIcon
