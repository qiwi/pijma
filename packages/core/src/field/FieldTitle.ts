import styled from '../styled'
import StyledComponent from '../StyledComponent'
import Theme from '../Theme'

const FieldTitle: StyledComponent<{}, {}, Theme> = styled('div')({
  position: 'relative',
  height: 16,
})

export default FieldTitle
